# Tagma Benchmark Results

This directory contains benchmark output from `benches/run.sh`.

Each run produces:
- A raw text log: `output-{timestamp}.txt`
- A condensed JSON summary: `bench-{timestamp}-{commit_hash}.json`

These files are gitignored (generated artifacts). See `.gitignore` at the workspace root.
